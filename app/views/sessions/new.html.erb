<article class="flex flex-col h-9/12 w-3/12 items-center justify-center gap-5 py-3 bg-darkLilac">
  <h1>Iniciar sesión</h1>

  <%= form_with url: session_path, method: :post, local: true, html: { class: 'flex flex-col gap-3' } do |form| %>
    <div class="flex flex-col gap-2">
      <%= form.label :email %>
      <%= form.text_field :email, required: true %>
    </div>

    <div class="flex flex-col gap-2">
      <%= form.label :password %>
      <%= form.password_field :password, required: true %>
    </div>

  <div class="flex items-center justify-center">
    <%= form.submit "Ingresar", class: "bg-darkPurple text-white" %>
  </div>
  <% end %>
</article>